#ifndef SORT_INCLUDE
#define SORT_INCLUDE

/*void quickSort(IntCount* begin, IntCount* end, Cmp cmp){

}

void mergeSort(IntCount* begin, IntCount* end, Cmp cmp){

}*/

/*template<class T, template<typename> class Cmp>
void BubbleSort(T* begin, T* end, Cmp<T> cmp) {
	for (T* curr1 = begin; curr1 != end; ++curr1) {
		for (T* curr2 = begin; curr2 != end; ++curr2) {
			if (cmp(curr2 + 1, curr2)) {
				T temp = *curr2;
				*curr2 = *(curr2 + 1);
				*(curr2 + 1) = temp;
			}
		}
	}
}*/

#endif